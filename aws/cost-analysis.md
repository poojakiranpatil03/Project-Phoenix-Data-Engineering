# AWS Cost Analysis

## Purpose

One of the key goals of Project Phoenix is to build an enterprise-inspired cloud data engineering platform while keeping infrastructure costs minimal.

This document explains the AWS services used, their pricing models, cost optimization techniques, and the expected monthly cost for learning purposes.

---

# Architecture Under Cost Analysis

                 Amazon S3
                      │
                      ▼
                Amazon SNS
                      │
                      ▼
          Snowflake Managed SQS
                      │
                      ▼
                 Snowpipe
                      │
                      ▼
              Snowflake RAW Layer

---

# AWS Services Used

| Service | Purpose | Pricing Model |
|----------|----------|---------------|
| Amazon S3 | Store incoming CSV files | Storage + Requests |
| Amazon SNS | Publish object creation events | Number of published messages |
| Amazon SQS | Queue notifications for Snowflake | Number of requests |
| IAM | Identity & Access Management | No additional cost |
| S3 Event Notifications | Trigger SNS on file upload | No additional cost |

---

# Service-wise Cost Analysis

## Amazon S3

### Purpose

Stores incoming source files before Snowflake ingests them.

### Pricing

Amazon S3 charges for:

- Storage used (GB per month)
- PUT requests
- GET requests
- Data transfer (when applicable)

### Project Phoenix Usage

- Small CSV files
- Few uploads per day
- Minimal storage consumption

Estimated Monthly Cost

Approximately ₹0–₹10 while using the AWS Free Tier.

### Cost Optimization

- Delete obsolete files periodically.
- Store only source data required for learning.
- Organize files using prefixes instead of creating multiple buckets.

---

## Amazon SNS

### Purpose

Broadcast file arrival notifications generated by Amazon S3.

### Pricing

Charged based on:

- Number of published messages
- Number of notification deliveries

### Project Phoenix Usage

- One notification generated per uploaded file.
- Very low message volume.

Estimated Monthly Cost

Typically ₹0 within the AWS Free Tier.

### Cost Optimization

- Publish only meaningful events.
- Filter unnecessary notifications using prefixes and suffixes.

---

## Amazon SQS

### Purpose

Provide reliable message delivery between Amazon SNS and Snowflake.

### Pricing

Charged based on:

- Number of API requests

### Project Phoenix Usage

Snowflake consumes messages automatically after file uploads.

Estimated Monthly Cost

Typically ₹0 within the AWS Free Tier.

### Cost Optimization

- Avoid unnecessary polling.
- Delete messages after successful processing (handled automatically by Snowflake).

---

## IAM

### Purpose

Secure communication between Snowflake and AWS.

### Pricing

IAM has no additional charges.

Estimated Monthly Cost

₹0

---

## S3 Event Notifications

### Purpose

Automatically notify Amazon SNS whenever a new file is uploaded.

Pricing

No additional charge.

Estimated Monthly Cost

₹0

---

# Snowflake Cost Considerations

Although AWS costs remain minimal, Snowflake compute consumption should also be considered.

# Snowflake Warehouse Configuration

Project Phoenix uses a dedicated virtual warehouse to execute all SQL queries and data loading operations.

| Property | Configuration | Purpose |
|----------|---------------|---------|
| Warehouse Name | `RETAIL_WH` | Dedicated compute warehouse for Project Phoenix |
| Warehouse Size | `X-SMALL` | Suitable for learning workloads while minimizing compute costs |
| Auto Suspend | `60 Seconds` | Automatically suspends the warehouse after 60 seconds of inactivity to reduce costs |
| Auto Resume | `Enabled` | Automatically resumes the warehouse whenever a query is executed |
| Initially Suspended | `TRUE` | Prevents unnecessary compute consumption when the warehouse is created |
| Scaling Policy | `Standard` | Default scaling policy suitable for the project's workload |
| Auto Scaling | Not Configured | A single-cluster warehouse is sufficient for this learning project |

---

## Why This Configuration?

The warehouse has been intentionally configured to optimize cost while maintaining good performance.

### Benefits

- ✅ Minimizes compute charges by suspending automatically after inactivity.
- ✅ Eliminates the need for manual warehouse management through Auto Resume.
- ✅ X-Small warehouse provides sufficient compute power for learning and development workloads.
- ✅ A dedicated warehouse isolates project execution from other Snowflake workloads.

These settings minimize compute costs by ensuring that the warehouse runs only when required.

### Snowpipe

Snowpipe is a serverless service.

Charges are based on:

- Number of files processed
- Compute resources consumed during ingestion

For Project Phoenix, the ingestion volume is extremely small, making Snowpipe costs negligible.

---

# Cost Optimization Strategies

Project Phoenix follows several cloud cost optimization best practices:

✅ Use an X-Small Warehouse

✅ Enable AUTO_SUSPEND

✅ Enable AUTO_RESUME

✅ Process only incremental files

✅ Use Event-Driven Snowpipe instead of scheduled polling

✅ Store only small learning datasets

✅ Organize S3 data using prefixes

✅ Monitor Snowflake usage regularly

# Snowflake Cost Summary

| Component | Configuration | Cost Optimization |
|-----------|---------------|-------------------|
| Warehouse | `RETAIL_WH` | Dedicated warehouse for the project |
| Warehouse Size | `X-SMALL` | Lowest compute tier suitable for development |
| Compute Billing | Pay only while the warehouse is running | Auto Suspend reduces idle costs |
| Auto Suspend | 60 Seconds | Prevents unnecessary compute usage |
| Auto Resume | Enabled | Starts automatically only when required |
| Snowpipe | Serverless | Charges are based on files processed rather than warehouse uptime |
| Storage | Compressed Snowflake Storage | Small datasets keep storage costs minimal |

---

# Estimated Monthly Cost

| Service | Estimated Cost |
|----------|---------------:|
| Amazon S3 | ₹0 – ₹10 |
| Amazon SNS | ₹0 |
| Amazon SQS | ₹0 |
| IAM | ₹0 |
| Event Notifications | ₹0 |
| Snowpipe | Negligible |
| Snowflake Warehouse (Learning Usage) | Depends on active compute time |

For a learning environment with small datasets and infrequent uploads, the overall monthly infrastructure cost is expected to remain very low.

---

# Key Learnings

Building cloud-native data pipelines does not necessarily require expensive infrastructure.

By combining:

- Amazon S3
- Amazon SNS
- Amazon SQS
- Snowpipe
- Auto Suspend Warehouses

It is possible to design a scalable, event-driven architecture while keeping operational costs minimal.

Understanding cloud pricing is just as important as understanding cloud architecture, as cost optimization is a key responsibility of modern data engineers and cloud architects.

---

# References

- AWS Pricing Calculator
- Amazon S3 Pricing
- Amazon SNS Pricing
- Amazon SQS Pricing
- Snowflake Pricing Guide
